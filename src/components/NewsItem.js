// показывает новость
export default function NewsItem({ icon, text, link }) {
  return (
    // <a href={link}>
    //   <img src={icon} alt="" />
    //   {text}
    // </a>
    <div>{'<NewsItem />'}</div>
  );
}